#include <iostream>
using namespace std;

int transform(int a[], int start, int end)
{

    return 0;
}

int main()
{
    int n[20];
    int start = 0;
    int end = sizeof(n) / sizeof(n[0]);
    return 0;
}