#include <iostream>
using namespace std;
int main()
{
    int a, b;
    cin >> a >> b;
    while (a > b, 0) //中间的逗号有且的意思，括号中表达式的值为：1&0 == 0
    {
        cout << "a>b"
             << " ";
    }

    return 0;
}